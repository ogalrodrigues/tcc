from django.apps import AppConfig

class RastreabilidadeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "rastreabilidade"
    verbose_name = "Rastreabilidade"   # nome da seção no admin

